# Template for ~/.secrets.zsh — copy to ~/.secrets.zsh and fill in real values.
#   cp zsh/.secrets.zsh.example ~/.secrets.zsh
# This file is sourced by .zshrc. The real ~/.secrets.zsh is gitignored, never committed.

export FIGMA_API_KEY="figd_REPLACE_ME"
export GPR_TOKEN="gho_REPLACE_ME"
